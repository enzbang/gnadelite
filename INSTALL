Dependencies
------------

   * Morzhol (git submodules)
     Some simple helper routines.

Building
--------

Building Morzhol

   $ git submodule init
   $ git submodule update

   $ cd external-libs/morzhol
   $ make
   $ sudo make install

Building gnadelite

   $ make
   $ sudo make install
